{
  "name": "wechatpost",
  "displayName": "WeChatPost - WeChat Publisher",
  "description": "Publish Markdown directly to WeChat Official Accounts from VSCode",
  "version": "1.3.1",
  "publisher": "cygnus",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/cygnusyang/wechatpost.git"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:markdown",
    "onCommand:wechatpost.configurePublishOptions",
    "onCommand:wechatpost.preview"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "WeChatPost",
      "properties": {
        "wechatPublisher.defaultAuthor": {
          "type": "string",
          "default": "",
          "description": "Default author name used when creating a WeChat draft."
        },
        "wechatPublisher.digestLength": {
          "type": "number",
          "default": 120,
          "minimum": 0,
          "description": "Digest length extracted from markdown content."
        },
        "wechatPublisher.declareOriginal": {
          "type": "boolean",
          "default": true,
          "description": "Enable original declaration by default."
        },
        "wechatPublisher.enableAppreciation": {
          "type": "boolean",
          "default": true,
          "description": "Enable appreciation by default."
        },
        "wechatPublisher.defaultCollection": {
          "type": "string",
          "default": "智能体",
          "description": "Default collection name used in WeChat."
        },
        "wechatPublisher.publishDirectly": {
          "type": "boolean",
          "default": true,
          "description": "Publish directly by default; disable to save as draft."
        },
        "wechatPublisher.contentThemePreset": {
          "type": "string",
          "enum": [
            "classic",
            "magazine",
            "minimal"
          ],
          "default": "classic",
          "description": "Theme preset for article typography and layout."
        },
        "wechatPublisher.contentBodyFontSize": {
          "type": "number",
          "default": 16,
          "minimum": 12,
          "maximum": 22,
          "description": "Body text font size for uploaded article content."
        },
        "wechatPublisher.contentLineHeight": {
          "type": "number",
          "default": 1.85,
          "minimum": 1.2,
          "maximum": 2.4,
          "description": "Body text line height for uploaded article content."
        },
        "wechatPublisher.contentTextColor": {
          "type": "string",
          "default": "#1f2329",
          "description": "Body text color (HEX) for uploaded article content."
        },
        "wechatPublisher.contentHeadingColor": {
          "type": "string",
          "default": "#0f172a",
          "description": "Heading color (HEX) for uploaded article content."
        },
        "wechatPublisher.contentLinkColor": {
          "type": "string",
          "default": "#0969da",
          "description": "Link/accent color (HEX) for uploaded article content."
        }
      }
    },
    "commands": [
      {
        "command": "wechatpost.uploadToWeChat",
        "title": "WeChatPost: Publish Current Markdown to WeChat",
        "icon": {
          "light": "media/icons/upload-light.svg",
          "dark": "media/icons/upload-dark.svg"
        }
      },
      {
        "command": "wechatpost.logoutWeChat",
        "title": "WeChatPost: Sign Out of WeChat Session"
      },
      {
        "command": "wechatpost.configurePublishOptions",
        "title": "WeChatPost: Configure Publishing Options"
      },
      {
        "command": "wechatpost.preview",
        "title": "WeChatPost: Preview WeChat Article Layout"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "clean": "rm -rf out dist coverage *.vsix tsconfig.tsbuildinfo",
    "rebuild": "npm run clean && npm run compile",
    "package": "vsce package",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "playwright:install": "playwright install",
    "test:playwright": "playwright test",
    "playwright:codegen": "playwright codegen",
    "lint": "echo 'TODO: Add linting'",
    "format": "echo 'TODO: Add formatting'"
  },
  "dependencies": {
    "markdown-it": "^14.1.0",
    "mermaid": "^10.9.1",
    "playwright": "^1.59.1"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.80.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9",
    "typescript": "^5.9.3"
  }
}
